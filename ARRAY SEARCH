//**
* Searches an array of speed
* for the fastest (smallest) value.
* @param speed array of speeds
* @return doube the fastest speed found
*/
public double searche(double[] speed){
	int size = speed.lenght;
	double min = speed[0];
	for(int i=1; i<size ; i++){
		if(speed[i]<min){
			min = speed[i];
}
}
}
